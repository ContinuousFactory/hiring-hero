%br
%ol.breadcrumb
  %li
    %a(href="/app") Home
  %li.active
    Event List

%h3 Event List

= link "Add event", to: company_event_path(@conn, :new, @conn.assigns.current_organisation.id), class: "btn btn-raised btn-xs btn-warning"

%table.table.table-striped.table-hover.table-condensed
  %thead
    %tr
      %th Status
      %th Item
      %th Priority
      %th Due time
      %th
  %tbody
    %tr
      %td 1
      %td Column content
      %td Column content
      %td Column content
      %td.text-right(style="white-space: nowrap;")
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
    %tr
      %td 2
      %td Column content
      %td Column content
      %td Column content
      %td.text-right(style="white-space: nowrap;")
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
    %tr
      %td 3
      %td Column content
      %td Column content
      %td Column content
      %td.text-right(style="white-space: nowrap;")
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
    %tr
      %td 4
      %td Column content
      %td Column content
      %td Column content
      %td.text-right(style="white-space: nowrap;")
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
    %tr
      %td 5
      %td Column content
      %td Column content
      %td Column content
      %td.text-right(style="white-space: nowrap;")
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
    %tr
      %td 6
      %td Column content
      %td Column content
      %td Column content
      %td.text-right(style="white-space: nowrap;")
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
    %tr
      %td 7
      %td Column content
      %td Column content
      %td Column content
      %td.text-right
        = link to: company_event_path(@conn, :delete, @conn.assigns.current_organisation, 1), method: :delete, data: [confirm: "Are you sure?"], class: "small-trash" do
          %span.glyphicon.glyphicon-trash
        = end
